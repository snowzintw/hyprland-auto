# hyprland-auto — extras do instalador (Hyprland 0.54+, awww, wallpaper inicial)
# Carregado no fim de hyprland.conf

# Primeiro wallpaper (o rice do binnewbs só arranca o daemon; sem imagem o fundo fica preto)
exec-once = ~/.config/hypr/scripts/set-default-wallpaper.sh
